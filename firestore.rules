rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY: Allow all access to unblock development
    // The previous rules were too restrictive for the server-side API calls
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
